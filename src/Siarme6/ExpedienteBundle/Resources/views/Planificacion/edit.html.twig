{{ form_start(edit_form,  {'action': path('planificacion_edit', {'id': planificacion.id}), 'method': 'POST'}) }}
        {{ form_widget(edit_form) }}
              <input class="btn btn-danger"  type="submit" value="GUARDAR" />
{{ form_end(edit_form) }}