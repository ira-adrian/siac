{# Siarme\GeneralBundle\Resources\views\ItemSolicitado\modal_new.html.twig #}

{{ form_start(form,  {'action': path('itemsolicitado_new', {'id': itemSolicitado.tramite.id}), 'method': 'POST'}) }}
        {{ form_widget(form) }}
          <div class="text-right">
            <input class="btn btn-danger" onclick= "return confirm('¿Desea GUARDAR?');" type="submit" value="GUARDAR"/>
          </div>
{{ form_end(form) }}

