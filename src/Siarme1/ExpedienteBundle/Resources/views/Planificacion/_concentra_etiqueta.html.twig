      {# concentra con etiquetas label #}
<div role="presentation" class="dropdown" >
    <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
      <span id="etiqueta-concentra" class="etiqueta-concentra text-info">
          {% if planificacion.estado is empty or planificacion.estado == "0" %}<span class="text-muted">Ninguno</span>{% endif %}
          {% if planificacion.estado == 1 %}<span class="text-info">SÍ</span>{% endif %}
          {% if planificacion.estado == 2 %}<span class="text-danger">NO</span>{% endif %}
         <span class="caret"></span>
       </span>
    </a>
{# if (planificacion.oferta.expediente | default(null) | movimiento_estado(app.user)) and  planificacion.proceso.departamentoRm == app.user.departamentoRm  and not planificacion.proceso.adjudicado #}
{% if edit %}
    <ul class="dropdown-menu">
                <li><a class="btn-concentra" data-href="{{ path('planificacion_concentra', { 'id': planificacion.id, 'estado':0 }) }}" data-etiqueta= "<span class='text-muted'>Ninguno</span> <span class='caret'></span>">Ninguno</a></li>
                <li><a class="btn-concentra" data-href="{{ path('planificacion_concentra', { 'id': planificacion.id, 'estado':1 }) }}" data-etiqueta="<span class='text-info'>SÍ</span> <span class='caret'></span>" >SÍ</a></li>
                <li><a class="btn-concentra" data-href="{{ path('planificacion_concentra', { 'id': planificacion.id, 'estado':2 }) }}" data-etiqueta="<span class='text-danger'>NO</span> <span class='caret'></span>" >NO</a></li>
    </ul>
 {% endif %}
</div>
